const addedComment = $("<%= escape_javascript(render(@comment)) %>");
addedComment.hide();

$("#<%= dom_id(@comment.photo) %>_new_comment_form")
  .before(addedComment)
  .hide()
  .slideDown(250);

addedComment.slideDown(250);

$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val("");

console.log("<%= @comment.body %>")