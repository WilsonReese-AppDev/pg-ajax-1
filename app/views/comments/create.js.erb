$("#<%= dom_id(@comment.photo) %>_new_comment_form")
  .before("<%= escape_javascript(render(@comment)) %>")
  .hide()
  .slideDown(250);

$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val("");

console.log("<%= @comment.body %>")