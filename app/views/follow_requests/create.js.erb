$(document.getElementsByClassName("<%= dom_id(current_user)%>_<%= dom_id(@follow_request.recipient) %>_follow_request"))
  .replaceWith("<%= j(render "follow_requests/follow_unfollow", recipient: @follow_request.recipient) %>");
